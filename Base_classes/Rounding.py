from decimal import ROUND_UP
import math
from enum import Enum

class Rounding():

    def __init__(self, precision = 0) -> None:
        self.precision = precision
    
    def up(self, x):
        pass
